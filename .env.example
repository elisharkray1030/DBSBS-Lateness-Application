# Docker Compose reads this file before `docker compose up`.
# Copy it to `.env` and set SECRET_KEY to a per-host secret. Generate one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
